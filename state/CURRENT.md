# ShieldRate — Canonical Current State

Date: 2026-09-15
Workstream: `WINNING_INTELLIGENCE_V4`
Repository: `Faadil1/shieldrate`
Branch: `winning-intelligence-v4`
Status: `V4_CREDENTIAL_BLOCKTIME_RECOVERY_PROVIDER2_PENDING`

## Product thesis

**Prove you qualify for the work. Do not reveal why.**

Canonical flow:

`COMMIT → CONSENT → PRIVATE PROOF → QUALIFIED`

## Deployment gate — CLOSED

Current Midnight Preprod contract:

`c67fcd95e1620693817a1248bceda34e507d39416a7e9c3038ad89f9844513d2`

Do not redeploy.

## Provider 1 gate — INDEXED / CLOSED

Indexed state confirms provider id `1`, epoch `0`, expected public-key match. Provider registration tx/block metadata remain uncaptured and must not be invented.

## Commit-Before-Know request — INDEXED / CLOSED

Canonical job scope: `sr-wave1-canonical-2026-09-15-01`

Policy: `SR-WORK-02` / code `2`

- workRequestId: `971f6ab489418b29039ae945a9b197238da5eed6f00c394305cf12366e36892b`
- transaction id: `003cda886aeecf397418920ee7317c8dc7ee784ae0b4da742438c175451e4bf084`
- block height: `2568670`

The request is finalized and indexed before holder proof. Exact `requestExpiresAtMs` remains to be recovered from indexed request state.

## Latest qualification attempt — PRE-SUBMISSION TIMING FAILURE

The first private qualification attempt stopped during Compact execution with:

`Unexpected error executing scoped transaction '<unnamed>': Error: failed assert: credential issuance is in the future`

No qualification transaction was submitted and no nullifier/work receipt was written by this failed attempt.

The contract guard is:

`blockTimeGte(credential.issuedAtEpoch)`

The loaded provider-1 credential used `issuedAtEpoch = 1789522255905` (`2026-09-16T01:30:55.905Z`). The failure therefore means the Midnight execution block time was still earlier than that issuance timestamp.

This is not an SR-WORK-02 policy failure. The credential values still satisfy policy 2; the failure is the issuance-time guard.

## Provider-1 credential cannot be re-signed

The provider-1 issuer secret was randomly generated by `issue-demo-credential.mjs` and intentionally not printed/persisted. A corrected credential cannot honestly be signed under the indexed provider-1 public key.

Do not rotate/remove provider 1 and do not fabricate a replacement signature.

## Current recovery gate

`REGISTER_FIXED_SECRET_PROVIDER_2_WITH_BACKDATED_CREDENTIAL`

Required sequence:

1. Keep the same browser session, holder binding and deployed contract.
2. Generate a new local fixed `SHIELDRATE_ISSUER_SECRET`; never commit or paste it into Git/chat evidence.
3. Use provider id `2`, provider epoch `0`, and an explicitly backdated `SHIELDRATE_ISSUED_AT_EPOCH` (24h behind local current time) to issue a fresh credential for the same holder binding.
4. Register provider `2` exactly once and confirm indexed key + epoch `0`.
5. Import the matching provider-2 credential into Card 04.
6. Retry work request `971f6a...6892b` once if still fresh. If it has expired, create a new job scope (for example `sr-wave1-canonical-2026-09-15-02`) with policy `2`; do not reuse the immutable existing employer/job scope.
7. Capture successful qualification verification id + tx + block.
8. Independently confirm `workReceiptExists=true`.
9. Recover request expiry and provider metadata where possible, then lock the final network evidence bundle.

## Truth boundary

- Deployment READY is proven.
- Provider 1 is indexed.
- Commit-Before-Know request is finalized/indexed at block `2568670`.
- The failed proof attempt never reached submission.
- Full V4 `NETWORK_VERIFIED` remains pending successful private qualification + independently indexed work receipt.
- Never expose holder/admin/issuer secrets or raw credential data in Git evidence.

## TRACE gate

Do not reopen TRACE UI/UX until private proof → indexed receipt → evidence lock is complete.
